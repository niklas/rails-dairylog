require 'spec_helper'

describe Rails::Dairylog do
  it 'has a version number' do
    expect(Rails::Dairylog::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
